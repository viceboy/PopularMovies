include ':app'
rootProject.name = "PopularMovies"
buildCache {
    local {
        push = true
        enabled = true
        directory = new File(rootDir, 'build-cache')
    }
    remote(HttpBuildCache) {
        enabled = false
    }
}